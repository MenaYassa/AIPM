# AIPM Production Roadmap

**Assessment date:** 2026-08-24
**Repository state:** `main` and `origin/main` at `37d8a0ecca26f82f2a5bcfee54c26bee1e89bd70` (`feat: implement MC-6.13 Phase 4A composition`)
**Source of requirements:** attached `AIPM.md`, especially sections 2, 5, 7–11, 15, and 18.

## Current-state assessment

AIPM has a usable Python/Typer foundation with configuration, logging, project discovery, Docker and Compose providers, Git state models, health analyzers, backups, and an update command. The previous completion pass made the package installable, removed the undeclared Compose dependency, added deterministic unit coverage, and improved error handling. The repository is clean locally and contains no tracked virtual environment or generated bytecode.

The attached production brief changes the completion target in an important way: **`aipm update` is the primary product**, and it must become a safe management transaction for an existing VPS rather than a generic Compose restart command. The current update implementation is still too small for that responsibility. It performs a pre-update snapshot, blocks dirty Git repositories, rebuilds one custom runtime or one Compose project, and runs a compatibility health check. It does not yet build a complete update plan, support dry-run or explicit approval, analyze upstream changes, produce an audit record, or provide a deterministic rollback transaction.

The real infrastructure paths named in the brief (`/home/ubuntu/local-ai-packaged`, `/home/ubuntu/cloudflared`, `/home/ubuntu/openvpn`, `/home/ubuntu/color-mixer`, and `/home/ubuntu/product-tracker`) are absent from the sandbox. The legacy `update.sh`, `start_services.py`, and infrastructure Compose files were therefore not available for inspection. No connection to the real VPS was attempted, and no production state was changed. The implementation below is consequently designed from the stated contracts and local repository behavior; it must receive a read-only VPS inspection and the real operational scripts before production execution is enabled.

## Gap matrix

| Requirement from the production brief | Current state | Priority | Completion condition |
|---|---|---:|---|
| Management layer must leave infrastructure independent | The repository uses provider adapters and does not move infrastructure into AIPM. | Complete | Keep external-system calls behind providers. |
| CLI → Capability → Engine → Service → Provider flow | Most paths follow the layering, but update is coupled to presentation and lacks separate planner/executor/verifier components. | High | Make update planning and execution services injectable and independent of Rich output. |
| Health before and after update | Health-after compatibility checks exist; health-before is not part of the update transaction. | High | Capture both reports in an update plan/audit record. |
| Snapshot / restore point | Snapshot creation exists; restore tooling and automatic rollback do not. | High | Add a restore-point model and an explicit rollback boundary without overwriting live files silently. |
| Preflight, Git, Docker, and Compose analysis | Git planning exists in partial form; Docker/Compose checks are embedded in health or called only during execution. | High | Build a deterministic update plan before any state-changing operation. |
| Dry-run | Not implemented. | Critical | `aipm update PROJECT --dry-run` must never create snapshots, fetch, pull, restart, or write project state. |
| User approval | Not implemented; current update executes immediately. | Critical | Default execution must require approval, while `--yes` is explicit and auditable. |
| Safe Git handling | Dirty/conflict detection exists; no remote diff analysis, critical-file change review, or safe stash transaction. | Critical | Classify local/remote changes, block critical conflicts, preserve stashes on apply conflicts, and never discard work. |
| Update planner / executor / verifier / rollback manager | Not implemented as separate components. | High | Introduce small services around the existing provider and model abstractions. |
| Audit history | Logging exists, but there is no structured update report persisted as an artifact. | High | Write a redacted JSON audit report for each planned or executed update. |
| Production VPS integration | Not inspected in this sandbox. | Blocked | Perform read-only inspection using refreshed credentials and explicit user approval before any state-changing command. |
| Comprehensive and integration tests | Nine deterministic unit tests exist; no disposable Git-remote or Docker integration fixtures exist. | High | Add planner/executor tests first, then isolated integration fixtures. |
| Open-source release hygiene | README and package metadata exist; contribution guide, release process, and upgrade guide are absent. | Medium | Add `CONTRIBUTING.md`, `CHANGELOG.md`, release checklist, and security policy before publishing. |

## First implementation milestone

The first production milestone is **safe update planning and dry-run execution**. It should not touch the production VPS. It should add a typed `UpdatePlan` that records project identity, health-before summary, Git state, Compose/runtime actions, snapshot requirement, risk, reasons, and approval requirement. A planner should construct that model without side effects. A renderer/capability should display it. The update command should support:

```text
aipm update <project> --dry-run
aipm update <project> --yes
```

The default command should display the plan and stop unless `--yes` is provided. `--dry-run` should explicitly prohibit all state-changing operations. The executor should remain behind services/providers and should only run after the plan has passed deterministic safety checks and explicit approval. This milestone should also persist a redacted JSON audit record for dry-run and execution outcomes.

## Ordered roadmap

### P0 — Safe planning and approval

Implement `UpdatePlanner`, `UpdatePlan`, dry-run output, explicit approval, risk classification, and audit records. Replace ad-hoc update decisions with typed plan data. Ensure no provider mutation is reachable from dry-run.

### P0 — Git transaction safety

Extend Git analysis to classify critical infrastructure changes, remote divergence, conflicts, detached heads, existing stashes, and safe local changes. Implement a transaction that preserves the stash when application conflicts occur and reports the exact conflicting files.

### P1 — Update execution and verification

Split execution into snapshot, fetch/pull, image pull/build, runtime restart, and verification steps. Add a verifier that compares health-before and health-after reports and returns success, warning, or failure without treating every warning as a rollback condition.

### P1 — Explicit rollback and restore points

Define what a snapshot contains, how it is restored, and which resources are intentionally outside its scope. Implement a rollback manager that can restore AIPM-managed configuration and return the infrastructure to the last known operational state without silently deleting volumes or databases.

### P1 — Real VPS read-only integration

After credentials are rotated and the user explicitly requests access, inspect the production environment read-only: repository status, Compose configuration, services, images, volumes, networks, disk, memory, systemd status, `update.sh`, and `start_services.py`. Store only redacted operational metadata in audit artifacts. Do not run `compose up/down`, `git pull`, package upgrades, pruning, or deletion during inspection.

### P2 — Integration testing and release readiness

Add disposable Git remote fixtures and Docker Compose fixtures, then add CI for tests, packaging, static checks, and security scans. Complete contribution, upgrade, changelog, release, and security documentation. Verify that no keys, tokens, host-specific secrets, or generated environments are committed.

## Decision boundary

The project should not be declared production-ready merely because the CLI works locally. The first milestone is complete only when a dry-run is demonstrably side-effect-free, a normal update requires explicit approval, critical Git changes block safely, and every planned/executed update has a structured audit record. Real-VPS execution remains a separate, approval-gated milestone after read-only inspection of the actual infrastructure.

## References

[1]: https://github.com/MenaYassa/AIPM "AIPM repository"
[2]: https://docs.python.org/3/library/subprocess.html "Python subprocess documentation"


## Mission Control alignment update

**Updated:** 2026-08-23

This document remains the roadmap for the broader AIPM management product, especially safe `aipm update` transactions. It is not a replacement for the Mission Control roadmap in [`docs/MC-6_IMPLEMENTATION_PLAN.md`](docs/MC-6_IMPLEMENTATION_PLAN.md) or the current status ledger in [`docs/MC-6_STATUS.md`](docs/MC-6_STATUS.md).

The Mission Control read-only cockpit is implemented and pushed through **MC-6.8**. The current repository checkpoint is `37d8a0ecca26f82f2a5bcfee54c26bee1e89bd70`, which also contains landed MC-6.13 Phase 2 evidence normalization, Phase 3 deterministic advisor rules, and Phase 4A pure composition. Completed Mission Control work includes the MC-5 dashboard and read-only SQLite boundary, MC-5 Gate 2.1 staging validation, shared MC-6 contracts and frontend shell, Server/Host Intelligence, Docker intelligence, Project/Application Intelligence, allow-listed Systemd observation, bounded redacted Logs, and the pure advisor domain.

The next Mission Control milestone is MC-6.9 design/inspection only. MC-6.10 Settings posture and MC-6.11 TUI remain planned; MC-6.12 remains the future action-control boundary; and MC-6.13 Phases 4B–4E remain unauthorized and not started after the completed pure Phase 4A composition. None of those milestones authorize changes to the broader `aipm update` transaction.

The broader roadmap work remains independently tracked:

| Workstream | Status after current checkpoint |
|---|---|
| Update planner and typed risk plan | Local implementation exists; further production-grade planning and integration hardening remain. |
| Side-effect-free dry-run and explicit approval | Implemented locally and must remain protected by regression tests. |
| Critical Git transaction safety | Further integration coverage and operational validation remain. |
| Restore points and deterministic rollback | Explicit restore tooling and rollback-manager work remain. |
| Structured audit history | Local redacted audit records exist; broader release/integration validation remains. |
| Disposable Git/Docker integration fixtures | Remaining. |
| CI, release, contribution, upgrade, and security documentation | Remaining release-readiness work. |
| MC-6.13 Phase 2/3 advisor domain | Complete and pushed; pure normalization and deterministic rules only. Phase 4 composition, API/UI integration, LLM functionality, and actions remain unauthorized. |
| Real VPS inspection | Separate read-only, explicitly approved operation; no state-changing commands are implied. |

Mission Control does not perform update execution, remediation, or public exposure itself. The current dashboard path is an existing bridge ingress—Cloudflared container → `172.20.0.1:8788` → host nginx reverse proxy → loopback dashboard at `127.0.0.1:8787`—while AIPM remains loopback-bound, GET-only, and read-only. Separately supplied production verification reports successful health checks through the existing bridge and public endpoint, while telemetry commit `0ab4b0e859fff96add058cb3eb55e0ff408b1a83` reduced the previous retention-spin CPU to approximately 0% with healthy sampling. These are live operational facts, not repository changes made by the roadmap work. Orphan-process cleanup and configuration-twin deletion remain external VPS state. This ingress topology remains separate from any future AIPM management control plane.
